package com.ss.jb.two;

public interface Shape {
	Double calculateArea(); // Each shape that implements this interface can calculate its area
	void display(); // Each shape that implements this interface displays the shape's dimensions
}
